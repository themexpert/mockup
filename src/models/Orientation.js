export default class Orientation {
    constructor(originX, originY, width, height, frames) {
        this.originX = originX;
        this.originY = originY;
        this.width = width;
        this.height = height;
        this.frames = frames;
    }
}